# Local testing dependencies (không push lên GitHub)
aiohttp>=3.9.0
fake-useragent>=1.5.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Audit & Linter
mypy>=1.0.0
flake8>=7.0.0
bandit>=1.7.0

# Formatting (Refactor)
black>=24.0.0
autoflake>=2.0.0
isort>=5.0.0
